{
  "name": "sp2p-web",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "cp index.html receive.html style.css dist/ && esbuild src/main.ts --bundle --outfile=dist/app.js --sourcemap --target=es2020 --watch",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.25.0",
    "typescript": "5.5"
  },
  "dependencies": {
    "@types/qrcode": "^1.5.6",
    "fzstd": "^0.1.1",
    "qrcode": "^1.5.4"
  }
}

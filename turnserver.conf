# Minimal coturn configuration for SP2P
listening-port=3478
fingerprint
use-auth-secret
static-auth-secret=replace-me-with-a-real-secret
realm=sp2p
no-tls
no-dtls

# Production settings — uncomment and adjust:
# external-ip=YOUR_PUBLIC_IP
# min-port=49152
# max-port=65535
